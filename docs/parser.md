# Parser

Parsers consume stored raw HTML/JSON and produce source-faithful records. They do not perform HTTP requests.

This separation makes it possible to improve extraction logic and reprocess thousands of saved pages without recrawling the website.
